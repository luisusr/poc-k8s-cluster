apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Local copy of wazuh-kubernetes manifests.
# Source: https://github.com/wazuh/wazuh-kubernetes
# Images upgraded to v4.14.4 (2026-04-14) — required for agent compatibility.
# Manifest structure kept at v4.9.2 baseline; only image tags updated.
# Full upstream re-inline can be done at next major version bump.
resources:
  - upstream/
  - entrypoint-configmap.yaml